#include <Arduino.h>
#include <LilyGoWatch.h>
#include "Apptemplate.hpp"
#include "../UI/UI.hpp"

MainMenuApplication::~MainMenuApplication() {
    
}
MainMenuApplication::MainMenuApplication() {
    
}
bool MainMenuApplication::Tick() {
    if (millis() > nextRedraw ) {
        // draw code here
        nextRedraw=millis()+(1000/2); // tune your required refresh
        return true;
    }
    return false;
}